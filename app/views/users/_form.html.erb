<%= form_with(model: user, local: true, html: { class: "ui form" } ) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name, 'Nombre' %>
    <%= form.text_field :first_name, id: :user_first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name, 'Apellidos' %>
    <%= form.text_field :last_name, id: :user_last_name %>
  </div>

  <div class="field">
    <%= form.label :supply_code, 'Código de Suministro' %>
    <%= form.text_field :supply_code, id: :user_supply_code %>
  </div>

  <div class="field">
    <%= form.label :dni, 'DNI' %>
    <%= form.text_field :dni, id: :user_dni %>
  </div>

  <div class="field">
    <%= form.submit "Registrar mi Usuario", class: "ui primary fluid button" %>
  </div>

  <div class="ui horizontal divider">o</div>

  <%= link_to "Inicia Sesión", new_sessions_path, class: "ui button" %>
<% end %>
